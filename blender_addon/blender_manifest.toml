schema_version = "1.0.0"

# Distinct from the bundled wheel's top-level package (openrct2_vehicle_generator)
# so the add-on module path and the imported renderer never collide.
id = "openrct2vg"
version = "0.1.0"
name = "OpenRCT2 Vehicle Generator"
tagline = "Author and export OpenRCT2 ride vehicles from the scene"
maintainer = "Alex Parisi <alex.t.parisi@gmail.com>"
type = "add-on"

tags = ["Import-Export", "Render"]
blender_version_min = "4.2.0"

# NOTE: confirm the project's license. GPL is the conventional choice for an
# add-on that imports `bpy`; the bundled wheels carry Embree + TBB (Apache-2.0),
# whose license texts must ship alongside.
license = ["SPDX:GPL-3.0-or-later"]

# The extension targets these platforms; Blender installs the matching wheel.
platforms = ["windows-x64", "macos-arm64", "linux-x64"]

# Self-contained renderer wheels (CPython 3.11, Embree+TBB vendored in) produced
# by .github/workflows/wheels.yml. These files must be present under ./wheels/
# when running `blender --command extension build`.
wheels = [
    "./wheels/openrct2_vehicle_generator-0.1.0-cp311-cp311-win_amd64.whl",
    "./wheels/openrct2_vehicle_generator-0.1.0-cp311-cp311-macosx_11_0_arm64.whl",
    "./wheels/openrct2_vehicle_generator-0.1.0-cp311-cp311-manylinux_2_28_x86_64.whl",
]

[permissions]
files = "Read texture images and write the generated .parkobj"
